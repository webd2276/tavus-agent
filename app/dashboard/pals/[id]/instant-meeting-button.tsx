"use client";
import { useState } from "react";
export function InstantMeetingButton({ palId }: { palId: string }) { const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(false); async function createMeeting() { setLoading(true); setError(null); const response = await fetch(`/api/pals/${palId}/meet`, { method: "POST" }); const data = await response.json(); setLoading(false); if (!response.ok) return setError(data.error || "Unable to create meeting"); window.open(data.join_url, "_blank", "noopener,noreferrer"); } return <div><button onClick={createMeeting} disabled={loading} className="rounded border border-[#7FA890] px-4 py-2 text-sm text-[#7FA890] disabled:opacity-60">{loading ? "Creating…" : "Open instant meeting"}</button>{error && <p className="mt-2 text-sm text-[#E08585]">{error}</p>}</div>; }
