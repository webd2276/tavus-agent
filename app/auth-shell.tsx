"use client";

import type { FormEvent, ReactNode } from "react";
import { StatusPill, ZainMark } from "./zain-mark";

export function AuthShell({ title, onSubmit, error, loading, submit, alternate }: { title: string; onSubmit: (event: FormEvent<HTMLFormElement>) => void; error: string | null; loading: boolean; submit: string; alternate: ReactNode }) {
  return <main className="zain-shell zain-grid flex min-h-dvh items-center px-5 py-24 text-zain-ink"><div className="mx-auto grid w-full max-w-5xl gap-12 lg:grid-cols-[.8fr_1fr] lg:items-center"><aside className="hidden lg:block"><StatusPill>Secure console</StatusPill><h1 className="mt-6 font-display text-5xl font-semibold leading-tight tracking-tight">A calm control plane for every conversation.</h1><p className="mt-5 max-w-sm leading-7 text-zain-muted">Configure the intelligence, safeguards, and availability behind your video sales presence.</p><div className="mt-10 space-y-3 font-mono text-xs text-zain-muted"><p className="text-zain-sageLight">◆ Encrypted workspace session</p><p>◌ Persona-level guardrails</p><p>◌ Real-time meeting controls</p></div></aside><div className="mx-auto w-full max-w-md"><div className="mb-7 flex items-center justify-between"><ZainMark /><StatusPill>Secure console</StatusPill></div><form onSubmit={onSubmit} className="glass-panel flex flex-col gap-5 rounded-[1.25rem] p-6 md:p-8"><div><p className="eyebrow mb-2">Workspace access</p><h2 className="font-display text-3xl font-semibold tracking-tight">{title}</h2><p className="mt-2 text-sm leading-6 text-zain-muted">Manage your autonomous video sales personas.</p></div><label className="text-sm font-medium text-zain-muted">Email<input required name="email" type="email" autoComplete="email" className="field mt-2" /></label><label className="text-sm font-medium text-zain-muted">Password<input required name="password" type="password" minLength={6} autoComplete="current-password" className="field mt-2" /></label>{error && <p className="rounded-lg border border-zain-dangerSurface/60 bg-zain-dangerSurface/20 px-3 py-2 text-sm text-zain-danger" role="alert">{error}</p>}<button disabled={loading} className="button-primary disabled:cursor-wait disabled:opacity-60">{loading ? "Please wait…" : submit}</button><p className="text-sm text-zain-muted">{alternate}</p></form></div></div></main>;
}
